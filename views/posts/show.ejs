<!DOCTYPE html>
<html>

<head>
    <%- include('../partials/head') %>
</head>

<body>
    <%- include('../partials/nav') %>
    <div class="container mb-3">
        <h2 class="boardTitle mb-3">Board</h2>
        <table>
            <thead>
                <tr>
                    <th>제목</th>
                    <td><%= post.title %></td>
                    <th>작성시간</th>
                    <td data-date-time="<%= post.createdAt %>"></td>
                </tr>
                <tr>
                    <th colspan="4">내용</th>
                </tr>
            </thead>
            <tbody>
                <tr>
                    <td colspan="4" rowspan="6"><%= post.body %></td>
                </tr>
            </tbody>
        </table>
        <div class="mt-3" style="float:right">
            <a class="btn" href="/posts">Back</a>
            <a class="btn" href="/posts/<%= post._id %>/edit">Edit</a>
            <form action="/posts/<%= post._id %>?_method=delete" method="post" class="d-inline">
                <a class="btn" href="javascript:void(0)"
                    onclick="confirm('Do you want to delete this?')?this.parentElement.submit():null;">Delete</a>
            </form>
        </div>
    </div>
</body>

</html>